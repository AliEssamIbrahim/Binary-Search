void sort ( int search[] , int size ) 
{
	for ( int i = 0 ; i < size ; i++ )
	{
		int temp = 0 ;
	    for ( int j = i ; j < size-1 ; j++ )
			if ( search[j+1] < search[j] )
			{
				temp = search[j+1] ;
				search[j+1] = search[j] ;
				search[j] = temp ;
			}
	}
}
